import { defaultStyles } from '../constants'

export const initialState = {
  title: 'New Table',
  colState: {},
  rowState: {},
  dataState: {},
  currentText: '',
  tableStyles: {},
  currentStyles: defaultStyles,
}